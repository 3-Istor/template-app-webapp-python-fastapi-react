version: '3.8'

services:
  backend:
    build: ./backend
    image: ghcr.io/3-lstor/template-app-webapp-python-fastapi-react/backend:latest
    container_name: fastapi-backend
    ports:
      - "8081:8000"
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    build: ./frontend
    image: ghcr.io/3-lstor/template-app-webapp-python-fastapi-react/frontend:latest
    container_name: react-frontend
    ports:
      - "3001:3001"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
